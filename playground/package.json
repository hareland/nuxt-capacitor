{
  "name": "playground",
  "type": "module",
  "private": true,
  "scripts": {
    "prepare": "nuxt prepare",
    "dev": "nuxt dev --extends nuxt-capacitor",
    "build": "nuxt build --extends nuxt-capacitor",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "mobile": "nuxt generate && cap sync",
    "mobile:ios": "pnpm mobile && cap open ios",
    "mobile:android": "pnpm mobile && cap open android"
  },
  "dependencies": {
    "@capacitor-community/electron": "^5.0.1",
    "@capacitor/android": "^8.1.0",
    "@capacitor/cli": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "@capacitor/ios": "^8.1.0",
    "nuxt": "^4.3.1",
    "nuxt-capacitor": "workspace:*",
    "vue": "^3.5.28",
    "vue-router": "^4.6.4"
  }
}
